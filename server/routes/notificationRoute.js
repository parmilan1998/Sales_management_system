const express = require("express");

// const setFcmToken = require("../controllers/notificationController");

const router = express.Router();

// router.post("/", setFcmToken);

module.exports = router;
